let username = "John"

function showMessage() {
	username = "Test"
	let message = "Hello" + " " + username
	alert(message)
}

function showText(from, text="Text not passed:(") {
	result = "Hello from" + " " + from + " " + text
	alert(result)
}

function sum(a, b) {
	return a + b
}

